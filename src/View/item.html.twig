{% extends 'layout.html.twig' %}

{% block title %}All items{% endblock %}
{% block content %}
    <section>
        <h1>Items</h1>
        <ul>
        {% for item in items %}
            <li><a href="/item/{{ item.id }}">{{ item.title }}</a> <a href="/items/edit/{{ item.id }}" >Editer </a><a href="/items/delete/{{ item.id }}"> Supprimer</a></li>
        {% endfor %}
        </ul>
        <p><a href="/items/add">Ajouter un item</a></p><br>
        <p><a href="/categories">Voir les categories</a></p>
    </section>
{% endblock %}